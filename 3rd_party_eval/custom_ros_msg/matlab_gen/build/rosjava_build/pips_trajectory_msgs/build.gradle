/*
 * Copyright 2015 The MathWorks, Inc.
 */

try {
    project.catkin.tree.generateMessageArtifact(project, 'pips_trajectory_msgs')
} catch (NullPointerException e) {
    println("Couldn't find pips_trajectory_msgs on the ROS_PACKAGE_PATH")
}

task bugfixtask << {
    mkdir sourceSets.main.output.classesDir
}
jar.dependsOn(bugfixtask)
